{
  "name": "mcsdev",
  "version": "0.1.0-dev",
  "description": "Minecraft Server Dev — terminal test-server environment for Paper/Folia plugin developers (v0.1 prototype)",
  "private": true,
  "license": "MIT",
  "bin": {
    "mcsdev": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "dev": "tsx src/cli.ts",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.17"
  }
}